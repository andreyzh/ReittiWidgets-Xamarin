<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- Base application theme. -->
  <style name="AppTheme" parent="@android:Theme.Material">
    <item name="android:actionBarStyle">@style/ActionBar</item>
    <item name="android:actionModeStyle">@style/ContextActionBar</item>
    <item name="android:windowBackground">@color/window_background</item>
    <!--item name="android:colorBackground">@color/window_background</item-->
  </style>
  
  <!-- ActionBar styles -->
  <style name="ActionBar" parent="@android:style/Widget.Material.ActionBar">
    <item name="android:background">@color/action_bar</item>
  </style>

  <!-- Context ActionBar -->
  <style name="ContextActionBar" parent="@android:style/Widget.Material.ActionMode">
    <item name="android:background">@color/context_action_bar</item>
  </style>
</resources>